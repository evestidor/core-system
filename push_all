#!/bin/bash
set -e

cd ui && git checkout master && git pull origin master
cd api-gateway && git checkout master && git pull origin master
cd svc-stock-manager && git checkout master && git pull origin master

./ui/config/push-image
./api-gateway/config/push-image
./svc-stock-manager/config/push-image
